{
  "manage_group": {
    "description": "管理群",
    "type": "string",
    "default": "",
    "hint": "投稿审核通知群；不填则尝试私发管理员。"
  },
  "pillowmd_style_dir": {
    "description": "Pillowmd 样式目录路径",
    "type": "string",
    "default": "",
    "hint": "例如 C:\\styles\\my-style；留空则使用普通文本/内置发布结果渲染。"
  },
  "llm": {
    "description": "大模型配置",
    "type": "object",
    "items": {
      "post_provider_id": {
        "description": "写说说用的大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "留空时使用当前会话默认提供商。"
      },
      "post_prompt": {
        "description": "写说说提示词",
        "type": "text",
        "default": "找出一个你感兴趣的主题来写一段适合 QQ 空间的说说，简短、有个性、不要解释。",
        "hint": "用于 写说说/写稿 和定时自动发说说。"
      },
      "news_provider_id": {
        "description": "新闻说说用的大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "留空时优先使用写说说提供商，再回退当前会话默认提供商。"
      },
      "news_prompt": {
        "description": "新闻说说提示词",
        "type": "text",
        "default": "从候选新闻中选一条你觉得适合发 QQ 空间的话题，写一段原创短评。不要直接复制新闻标题，不要贴链接，不要编造标题之外的细节，简短、有观点、像自然说说。",
        "hint": "用于定时谷歌新闻 RSS 新闻说说和 新闻说说预览。"
      },
      "comment_provider_id": {
        "description": "评论说说用的大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "留空时使用当前会话默认提供商。"
      },
      "comment_pipeline_enabled": {
        "description": "自动评论决策流水线",
        "type": "bool",
        "default": true,
        "hint": "开启后，定时自动评论会先经过判断、推理和执行三个阶段。"
      },
      "comment_judgment_provider_id": {
        "description": "自动评论判断阶段大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "可选；用于判断一条说说是否适合评论。"
      },
      "comment_reasoning_provider_id": {
        "description": "自动评论推理阶段大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "可选；用于在写评论前规划关系立场和语气。"
      },
      "comment_execution_provider_id": {
        "description": "自动评论执行阶段大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "可选；用于生成最终评论。留空时使用评论说说提供商。"
      },
      "comment_skip_checkins": {
        "description": "跳过低信息量打卡动态",
        "type": "bool",
        "default": true,
        "hint": "判断阶段会跳过较短的打卡式说说，减少无意义评论。"
      },
      "comment_prompt": {
        "description": "评论说说提示词",
        "type": "text",
        "default": "生成一句简短、直接、贴题的评论，不要解释。",
        "hint": "用于 评说说/评论说说/读说说 和自动评论。"
      },
      "comment_max_length": {
        "description": "评论最大长度",
        "type": "int",
        "default": 60,
        "slider": {
          "min": 10,
          "max": 200,
          "step": 1
        },
        "hint": "AI 生成评论和回评时会截断到这个长度。"
      },
      "reply_provider_id": {
        "description": "回复评论用的大模型提供商",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "留空时使用当前会话默认提供商。"
      },
      "reply_prompt": {
        "description": "回复评论提示词",
        "type": "text",
        "default": "这条帖子收到了一条评论，请自然回复此条评论，不要解释。",
        "hint": "用于 回评/回复评论。"
      }
    }
  },
  "source": {
    "description": "数据来源配置",
    "type": "object",
    "items": {
      "ignore_groups": {
        "description": "忽略的群聊列表",
        "type": "list",
        "default": [],
        "hint": "概率触发读说说/评论时会跳过这些群。"
      },
      "ignore_users": {
        "description": "忽略的用户列表",
        "type": "list",
        "default": [],
        "hint": "概率触发读说说/评论时会跳过这些用户。"
      },
      "post_max_msg": {
        "description": "用于写说说的聊天记录最大条数",
        "type": "int",
        "default": 500,
        "slider": {
          "min": 100,
          "max": 1000,
          "step": 10
        },
        "hint": "预留给后续聊天记录上下文聚合。"
      }
    }
  },
  "trigger": {
    "description": "触发配置",
    "type": "object",
    "items": {
      "publish_cron": {
        "description": "自动发说说的基准触发时间",
        "type": "string",
        "default": "",
        "hint": "定时表达式：分 时 日 月 周。留空表示禁用。"
      },
      "publish_offset": {
        "description": "自动发说说的随机偏移秒数",
        "type": "int",
        "default": 0,
        "slider": {
          "min": 0,
          "max": 3600,
          "step": 1
        },
        "hint": "围绕基准时间随机前后浮动；0 表示严格按基准时间。"
      },
      "news_cron": {
        "description": "新闻自动发说说的基准触发时间",
        "type": "string",
        "default": "",
        "hint": "定时表达式：分 时 日 月 周。留空表示禁用；例如 30 8 * * * 表示每天 8:30。"
      },
      "news_offset": {
        "description": "新闻自动发说说的随机偏移秒数",
        "type": "int",
        "default": 0,
        "slider": {
          "min": 0,
          "max": 3600,
          "step": 1
        },
        "hint": "围绕新闻发布基准时间随机前后浮动；0 表示严格按基准时间。"
      },
      "comment_cron": {
        "description": "自动评论的基准触发时间",
        "type": "string",
        "default": "",
        "hint": "定时表达式：分 时 日 月 周。留空表示禁用。"
      },
      "comment_offset": {
        "description": "自动评论的随机偏移秒数",
        "type": "int",
        "default": 0,
        "slider": {
          "min": 0,
          "max": 3600,
          "step": 1
        },
        "hint": "围绕基准时间随机前后浮动；0 表示严格按基准时间。"
      },
      "comment_latest_count": {
        "description": "每次自动评论的最新说说条数",
        "type": "int",
        "default": 1,
        "slider": {
          "min": 1,
          "max": 20,
          "step": 1
        },
        "hint": "每次定时自动评论会从好友最新动态中选择未评论过的说说，跳过自己的动态和已评论过的动态。"
      },
      "read_prob": {
        "description": "消息触发读说说概率",
        "type": "float",
        "default": 0.0,
        "hint": "收到 OneBot 消息时，按概率查看发送者空间并自动评论；0 表示关闭。"
      },
      "send_admin": {
        "description": "自动任务后反馈管理员",
        "type": "bool",
        "default": false,
        "hint": "开启后，定时发布、定时评论会向管理群或管理员私聊发送结果和 QQ 空间风格渲染图；收到消息概率触发的自动评论会发回当前会话。"
      },
      "like_when_comment": {
        "description": "评论时同时点赞",
        "type": "bool",
        "default": false,
        "hint": "评说说和自动评论成功后是否顺手点赞。"
      }
    }
  },
  "news": {
    "description": "谷歌新闻 RSS 新闻说说配置",
    "type": "object",
    "items": {
      "scopes": {
        "description": "新闻范围",
        "type": "list",
        "default": [
          "china"
        ],
        "hint": "可填 中国/国际/混合，也兼容 china/world/mixed；混合会同时获取中国和国际新闻。"
      },
      "keywords": {
        "description": "新闻关键词",
        "type": "list",
        "default": [],
        "hint": "可选；每个关键词会额外请求谷歌新闻 RSS 搜索源。"
      },
      "custom_rss_urls": {
        "description": "自定义谷歌新闻 RSS 地址",
        "type": "list",
        "default": [],
        "hint": "可选；仅允许谷歌新闻 RSS 地址，必须以 https://news.google.com/rss 开头。"
      },
      "max_candidates": {
        "description": "候选新闻数量",
        "type": "int",
        "default": 12,
        "slider": {
          "min": 1,
          "max": 30,
          "step": 1
        },
        "hint": "每次交给大模型选择和改写的最新新闻候选数量。"
      },
      "recency_hours": {
        "description": "新闻新鲜度小时数",
        "type": "int",
        "default": 36,
        "slider": {
          "min": 1,
          "max": 168,
          "step": 1
        },
        "hint": "只使用这个时间范围内的 RSS 新闻；0 表示不按发布时间过滤。"
      },
      "once_per_day": {
        "description": "每天最多发布一次新闻说说",
        "type": "bool",
        "default": true,
        "hint": "开启后，同一天新闻定时任务只会成功发布一次。"
      },
      "max_post_length": {
        "description": "新闻说说最大长度",
        "type": "int",
        "default": 180,
        "slider": {
          "min": 40,
          "max": 500,
          "step": 10
        },
        "hint": "大模型生成的新闻说说会截断到这个长度。"
      },
      "trust_env": {
        "description": "RSS 请求使用系统代理",
        "type": "bool",
        "default": true,
        "hint": "仅影响谷歌新闻 RSS 请求，不影响 QQ 空间 Cookie 请求；国内环境通常需要开启系统代理。"
      }
    }
  },
  "cookies_str": {
    "description": "Cookie 字符串",
    "type": "text",
    "default": "",
    "hint": "可选；填写后插件初始化时会尝试直接绑定 Cookie。也可以继续使用 /qzone bind。"
  },
  "timeout": {
    "description": "QQ 空间请求超时",
    "type": "float",
    "default": 15.0,
    "hint": "兼容旧版超时配置；会映射到请求超时时间。"
  },
  "show_name": {
    "description": "展示投稿者昵称",
    "type": "bool",
    "default": true,
    "hint": "匿名投稿仍会隐藏投稿者。"
  },
  "daemon_port": {
    "description": "本地守护进程端口",
    "type": "int",
    "default": 18999,
    "hint": "本地守护进程监听端口。"
  },
  "keepalive_interval": {
    "description": "本地守护进程保活间隔",
    "type": "int",
    "default": 120,
    "hint": "本地守护进程保活间隔，单位秒。"
  },
  "request_timeout": {
    "description": "请求超时时间",
    "type": "float",
    "default": 15.0,
    "hint": "向 QQ 空间和本地守护进程发请求的超时时间。"
  },
  "start_timeout": {
    "description": "本地守护进程启动等待时间",
    "type": "float",
    "default": 20.0,
    "hint": "等待本地守护进程启动成功的最长时间。"
  },
  "public_feed_limit": {
    "description": "默认说说拉取数量",
    "type": "int",
    "default": 5,
    "hint": "未指定条数时默认拉取的说说数量。"
  },
  "max_feed_limit": {
    "description": "单次最多拉取说说数量",
    "type": "int",
    "default": 20,
    "hint": "单次最多允许拉取的说说数量。"
  },
  "auto_start_daemon": {
    "description": "自动启动本地守护进程",
    "type": "bool",
    "default": true,
    "hint": "第一次使用时自动启动本地守护进程。"
  },
  "auto_bind_cookie": {
    "description": "自动绑定 Cookie",
    "type": "bool",
    "default": true,
    "hint": "插件初始化、AstrBot 加载或首次捕获 aiocqhttp 客户端时，若登录态缺失会尝试从 OneBot 自动获取 Cookie；自动绑定最多重试 3 次，仍失败时请使用 /qzone autobind 或 /qzone bind。"
  },
  "cookie_domain": {
    "description": "OneBot Cookie 域名",
    "type": "text",
    "default": "user.qzone.qq.com",
    "hint": "向 OneBot 请求 Cookie 时使用的域名。"
  },
  "admin_uins": {
    "description": "管理员 QQ 号",
    "type": "text",
    "default": "",
    "hint": "多个 QQ 号用英文逗号分隔。"
  },
  "user_agent": {
    "description": "自定义请求标识",
    "type": "text",
    "default": "",
    "hint": "自定义 QQ 空间请求头里的浏览器标识。"
  },
  "render_publish_result": {
    "description": "发布成功后渲染结果图",
    "type": "bool",
    "default": true,
    "hint": "发布说说成功后返回 QQ 空间风格渲染图。"
  },
  "render_result_width": {
    "description": "发布结果图宽度",
    "type": "int",
    "default": 900,
    "hint": "发布结果渲染图宽度。"
  },
  "render_feed_card_limit": {
    "description": "说说卡片渲染数量上限",
    "type": "int",
    "default": 5,
    "slider": {
      "min": 1,
      "max": 20
    },
    "hint": "看/读/评/赞说说时单次最多渲染的说说卡片数量，避免范围过大拖慢响应。"
  },
  "render_remote_timeout": {
    "description": "发布结果图远程资源超时",
    "type": "float",
    "default": 0.35,
    "hint": "发布结果图远程头像或图片预览的单资源超时。"
  }
}
